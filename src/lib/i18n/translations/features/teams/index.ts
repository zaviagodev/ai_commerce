import { members } from './members';
import { invites } from './invites';

export const teams = {
  en: {
    members: members.en,
    invite: invites.en,
  },
  th: {
    members: members.th,
    invite: invites.th,
  },
};
